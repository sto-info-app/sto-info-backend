import { SesEventEntity } from './ses-event.entity';

describe('SesEventEntity', () => {
  it('should be defined', () => {
    const entity = new SesEventEntity();
    expect(entity).toBeDefined();
  });
});
