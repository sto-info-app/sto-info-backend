import { UserRefreshTokenModule } from './user-refresh-token.module';

describe('UserRefreshTokenModule', () => {
  it('should be defined', () => {
    expect(new UserRefreshTokenModule()).toBeDefined();
  });
});
