/**
 * What a Character portrait is recorded as in Cloudflare Images.
 *
 * Unchanged from the string the synchronous upload path passed. It is part
 * of the custom identifier and the metadata of every portrait already
 * stored, so changing it would divide the estate into portraits uploaded
 * before FC-012 and portraits uploaded after it.
 */
export const CHARACTER_IMAGE_ENTITY_TAG = 'character';
