import { PlatformLauncherModule } from './platform-launcher.module';

describe('PlatformLauncherModule', () => {
  it('should be defined', () => {
    expect(new PlatformLauncherModule()).toBeDefined();
  });
});
