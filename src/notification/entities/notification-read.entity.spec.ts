import { NotificationReadEntity } from './notification-read.entity';

describe('NotificationReadEntity', () => {
  it('should be defined', () => {
    expect(new NotificationReadEntity()).toBeDefined();
  });
});
